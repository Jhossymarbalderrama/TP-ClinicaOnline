import { FiltroTurnosXNamePacientePipe } from './filtro-turnos-xname-paciente.pipe';

describe('FiltroTurnosXNamePacientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosXNamePacientePipe();
    expect(pipe).toBeTruthy();
  });
});

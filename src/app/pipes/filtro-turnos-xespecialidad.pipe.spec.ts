import { FiltroTurnosXEspecialidadPipe } from './filtro-turnos-xespecialidad.pipe';

describe('FiltroTurnosXEspecialidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosXEspecialidadPipe();
    expect(pipe).toBeTruthy();
  });
});

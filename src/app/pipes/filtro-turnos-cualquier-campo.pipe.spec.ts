import { FiltroTurnosCualquierCampoPipe } from './filtro-turnos-cualquier-campo.pipe';

describe('FiltroTurnosCualquierCampoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosCualquierCampoPipe();
    expect(pipe).toBeTruthy();
  });
});

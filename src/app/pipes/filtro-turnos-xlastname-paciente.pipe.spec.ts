import { FiltroTurnosXLastnamePacientePipe } from './filtro-turnos-xlastname-paciente.pipe';

describe('FiltroTurnosXLastnamePacientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosXLastnamePacientePipe();
    expect(pipe).toBeTruthy();
  });
});
